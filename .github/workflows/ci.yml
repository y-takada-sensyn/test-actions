name: test

on:
  push:

jobs:
  CodeQualityClient:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: run linter
        run: echo lint completed

  CodeQualityApi:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      LARASTAN_VERSION: ^0.7.12
    steps:
    - uses: actions/checkout@v2
    - uses: shivammathur/setup-php@v2
      with:
        php-version: '7.4'
        tools: composer:v2
    - name: Get composer cache directory path
      id: composer-cache-dir-path
      run: |
        echo "::set-output name=dir::$(composer config cache-files-dir)"
    - name: Cache larastan install
      uses: actions/cache@v2
      with:
        path: ${{ steps.composer-cache-dir-path.outputs.dir }}
        key: ${{ runner.os }}-larastan-${{ env.LARASTAN_VERSION }}
        restore-keys: |
          ${{ runner.os }}-larastan-
    - name: Install larastan
      working-directory: api
      run: composer require --dev --no-progress --prefer-dist nunomaduro/larastan:$LARASTAN_VERSION
    - name: Run larastan
      working-directory: api
      run: ./vendor/bin/phpstan --memory-limit=1G analyse

  Setup:
    needs: [CodeQualityClient, CodeQualityApi]
    runs-on: ubuntu-latest
    steps:
    - name: Init
      run: |
        echo setup completed
